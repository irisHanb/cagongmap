-- ============================================================================
--  카공맵 — (폐기됨) submission-images 버킷
-- ----------------------------------------------------------------------------
--  이 마이그레이션은 2026-08-20 오전에 검수 전 사진을 담을 **비공개 버킷**
--  submission-images와 그 storage 정책 셋을 만들었다. 같은 날 오후에 결정이
--  바뀌었다 — 카페 관련 이미지는 전부 place-images 하나를 쓴다
--  (20260820121437_submission_photos_public.sql).
--
--  그래서 내용을 비웠다. 파일과 버전은 남긴다: 이미 적용된 프로젝트의 기록이고,
--  지우면 db push가 적용 이력과 어긋난다.
--
--  ⚠️ 이미 적용된 프로젝트에는 이 파일을 비워도 버킷이 그대로 남는다. 마이그레이션은
--     한 번만 돌기 때문이다. **Postgres가 storage 테이블의 직접 삭제를 막으므로
--     (storage.protect_delete) SQL로는 지울 수 없다.** 대시보드에서
--     Storage → submission-images → Delete로 지운다. 앱은 이 버킷을 더 이상
--     참조하지 않으므로 남아 있어도 동작에는 영향이 없다.
--
--  그때 만들었던 정책 셋은 20260820121437이 drop policy if exists로 걷어낸다.
-- ============================================================================

-- 의도적으로 비어 있다.
